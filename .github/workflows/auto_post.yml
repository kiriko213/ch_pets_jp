name: AI Shorts Post (Pets JP)

on:
  schedule:
    # 日本時間 07:00, 18:00, 22:00 (UTC 22:00, 09:00, 13:00)
    - cron: '35 22 * * *'
    - cron: '5 9 * * *'
    - cron: '5 12 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-post:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg fonts-liberation fonts-noto-cjk

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Start VOICEVOX Engine
      run: |
        docker run --rm -d -p 50021:50021 voicevox/voicevox_engine:cpu-ubuntu20.04-latest
        sleep 15

    - name: Run Auto Post
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY_PETS_JP }}
        PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY_PETS_JP }}
        YOUTUBE_CLIENT_ID_PETS_JP: ${{ secrets.YOUTUBE_CLIENT_ID_PETS_JP }}
        YOUTUBE_CLIENT_SECRET_PETS_JP: ${{ secrets.YOUTUBE_CLIENT_SECRET_PETS_JP }}
        YOUTUBE_REFRESH_TOKEN_PETS_JP: ${{ secrets.YOUTUBE_REFRESH_TOKEN_PETS_JP }}
        GOOGLE_TTS_API_KEY: ${{ secrets.GOOGLE_TTS_API_KEY }}
        GITHUB_ACTIONS: "true"
        PYTHONUNBUFFERED: "1"
      run: |
        python auto_main.py



